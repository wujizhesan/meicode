# MeiCode 面试陈述

> 面试前翻这个文件。定位：**终端多智能体协作框架**（不提逆向）。
> 仓库：https://github.com/wujizhesan/meicode（开源版）

## 一、30 秒自我介绍（项目段）

> 我开发了一个叫 MeiCode 的**终端多智能体协作框架**：主会话可以把复杂任务拆解给多个 AI 专家成员并行执行——每个成员独立上下文、隔离工作区、邮箱协作、可跨会话恢复。整个项目 TypeScript 从零实现，包含 Agent 主循环、团队编排、Workflow 引擎、权限沙箱四个子系统，18 个测试文件全绿。

## 二、2 分钟创新点陈述（团队编排）

> 这个项目的核心不是"AI 能调工具"——那谁都能做——而是**怎么让多个 AI 可靠地协作完成复杂任务**。我做了三层：
>
> **第一层：隔离与共享的平衡**。每个成员跑在独立 git worktree 里，写文件不会污染主仓库；但中间产物通过共享区互通——一个成员抓的数据另一个成员能直接读。报告有"契约目录"，强制落盘，杜绝成果丢失。
>
> **第二层：编排保障机制**。我跑过 9 轮真实任务，每个机制都是一次失败换来的：成员异常死亡会发 ERR 邮件通知经理（之前成员静默消失，主会话空等 45 轮烧掉 10 万 tokens）；等待时有纪律——3 轮查一次、5 次无进展就降级自己收尾；轮次快用完时系统注入"报告优先"提醒（之前成员分析到最后一轮报告没写出来）。这些听起来琐碎，但**多 agent 系统 90% 的失败都死在这类"最后一公里"上**。
>
> **第三层：安全的路径沙箱**。成员只能写自己的工作区，读外部目标自由，命令有只读白名单——防绕过。Windows 上踩过一个很深的坑：junction 链接被递归删除时会把主仓库依赖一起清空，咬了我三次，最后用符号链接根治并补了回归测试。

## 三、亮点清单（追问备用）

| # | 亮点 | 证据 |
|---|---|---|
| 1 | 多智能体团队编排（协程成员/隔离/契约/保障机制） | src/team/ + 9 轮实战 |
| 2 | 路径围栏安全模型（读/写分离 + 只读白名单 + 防绕过） | src/tools/types.ts + 16 项测试 |
| 3 | Workflow 声明式 DSL + 双执行后端（fork / 团队协程） | src/workflow/ + 8 项测试 |
| 4 | 稳定性工程（上下文超限自动恢复/400 防线/重复调用检测） | src/agent/loop.ts |
| 5 | 实战驱动开发（每个机制对应一次真实失败） | 9 轮实战 5 目标闭环 |

## 四、常见追问应答

| 追问 | 应答 |
|---|---|
| "借鉴了 Claude Code 吧？" | 架构模式对齐了主流 agent 框架，但团队协作的工程化——成员生命周期、隔离、契约、保障机制——是完整自研，并且用真实任务验证过 |
| "规模多大？" | 205 个文件、18 个测试文件全绿、9 轮实战验证 5 个真实目标闭环 |
| "最难的点？" | 不是功能，是**可靠性**：上下文爆炸、死循环、成员失联、报告丢失——每个都是工程问题，我建了一整套防御机制 |
| "下一步？" | Workflow 与团队打通已做（phase 可选协程成员）；计划做插件化、上下文精简 |
| "为什么用 TypeScript？" | 类型安全 + 生态（Ink 终端 UI/react/playwright-core），Node 24 原生跑 TS 免构建 |

## 五、项目数据速查

- 代码：205 文件，TypeScript/ESM，Node ≥20
- 测试：18 文件全绿（team 12 / workflow 8 / loop 22 / audit 16 等）
- 子系统：agent / team / workflow / subagent / tools / permission / mcp / hook / worktree / tui / commands
- 架构模式：主循环（model→tools→result）+ 协程成员 + 邮箱协议（IDLE/PLAN/APPROVE）+ worktree 隔离 + 契约目录
